export const enum InteractionType {
  SlashCommand,
  Button,
  Select,
  ContextMenu,
  Modal,
  AutoComplete
}

export const enum ReportType {
  Webhook,
  Text
}

export const enum LevelType {
  Silly,
  Trace,
  Debug,
  Info,
  Warn,
  Error,
  Fatal
}
